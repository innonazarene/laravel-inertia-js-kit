export default function InputError({ message, className = '' }: { message?: string; className?: string }) {
    return message ? (
        <p className={'mt-1 text-sm text-red-600 ' + className}>{message}</p>
    ) : null;
}
